<h1>会員情報</h1>
 <div>
   アカウント名：<%= @customer.name %>
 </div>

 <% @cats.each do |cat| %>
 <div>
   <% if cat.cat_image.attached? %>
     <%= image_tag cat.cat_image, size: "100x100" %>
    <% else %>
     <%= image_tag 'no_image', size: "100x100" %>
    <% end %>
    猫ちゃんの名前：<%= link_to cat_path(cat.id) do %><%= cat.name %><% end %>ちゃん
 </div>
 <% end %>